{
  "name": "corepump-launchpad",
  "version": "1.0.0",
  "description": "CorePump - Fair Token Launchpad on Core Chain",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.ts",
    "deploy:local": "npx hardhat run scripts/deploy.ts --network localhost",
    "price-demo": "npx hardhat run scripts/testnet-price-demo.ts",
    "price-demo:local": "npx hardhat run scripts/testnet-price-demo.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.0.0",
    "hardhat": "^2.25.0"
  },
  "dependencies": {
    "@chainlink/contracts": "^1.4.0",
    "@openzeppelin/contracts-upgradeable": "^5.3.0",
    "@openzeppelin/hardhat-upgrades": "^3.9.1"
  }
}
